# 🚀 Быстрые исправления для бота на VPS

## 📋 Текущая ситуация

**Бот запущен:** `nohup node bot.js > app.log 2>&1 &`

**Проблемы:**
- ❌ Нет автоматического перезапуска при падении
- ❌ Нет мониторинга
- ❌ Нет управления процессом
- ❌ Логи не ротируются

---

## 🎯 Быстрое исправление (5 минут)

### Шаг 1: Установка PM2

```bash
# Подключитесь к VPS
ssh -i "d:\viAIzer\vps_bot_key" root@217.119.129.239

# Установите PM2 глобально
npm install -g pm2

# Проверьте установку
pm2 --version
```

### Шаг 2: Создание конфигурации PM2

```bash
# Перейдите в директорию проекта
cd /root/viaizer

# Создайте ecosystem.config.js
cat > ecosystem.config.js << 'EOF'
module.exports = {
  apps: [{
    name: 'viaizer-bot',
    script: 'bot.js',
    cwd: '/root/viaizer',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    env: {
      NODE_ENV: 'production'
    },
    error_file: '/root/viaizer/logs/error.log',
    out_file: '/root/viaizer/logs/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    time: true
  }]
};
EOF
```

### Шаг 3: Создание директории для логов

```bash
mkdir -p /root/viaizer/logs
```

### Шаг 4: Проверка .env файла

```bash
# Проверьте существует ли .env
if [ ! -f .env ]; then
  echo "⚠️ .env файл не найден!"
  echo "Создайте его из .env.example:"
  cp .env.example .env
  chmod 600 .env
  echo "✅ .env создан. Отредактируйте его с вашими ключами!"
  nano .env
else
  echo "✅ .env файл существует"
fi
```

### Шаг 5: Остановка текущего процесса

```bash
# Остановите текущий процесс
pkill -f "node.*bot.js"

# Подождите пару секунд
sleep 2

# Проверьте что процесс остановлен
ps aux | grep "node.*bot.js"
```

### Шаг 6: Запуск через PM2

```bash
# Запустите бота через PM2
pm2 start ecosystem.config.js

# Проверьте статус
pm2 status

# Посмотрите логи
pm2 logs viaizer-bot --lines 20
```

### Шаг 7: Сохранение конфигурации PM2

```bash
# Сохраните список процессов
pm2 save

# Настройте автозапуск при старте системы
pm2 startup systemd -u root --hp /root
```

---

## ✅ Проверка результата

```bash
# Проверьте что бот работает
pm2 status viaizer-bot

# Должно показать: online

# Проверьте логи
pm2 logs viaizer-bot

# Проверьте что бот отвечает в Telegram
# Отправьте /start вашему боту
```

---

## 📊 Полезные команды PM2

### Управление процессом

```bash
# Перезапуск бота
pm2 restart viaizer-bot

# Остановка бота
pm2 stop viaizer-bot

# Запуск бота
pm2 start viaizer-bot

# Удаление процесса из PM2
pm2 delete viaizer-bot
```

### Просмотр логов

```bash
# Просмотр логов в реальном времени
pm2 logs viaizer-bot

# Просмотр последних 100 строк
pm2 logs viaizer-bot --lines 100

# Просмотр только ошибок
pm2 logs viaizer-bot --err

# Сброс логов
pm2 flush viaizer-bot
```

### Мониторинг

```bash
# Мониторинг в реальном времени
pm2 monit

# Информация о процессе
pm2 show viaizer-bot

# Список всех процессов
pm2 list
```

---

## 🔄 Ротация логов

```bash
# Установка pm2-logrotate
pm2 install pm2-logrotate

# Настройка параметров
pm2 set pm2-logrotate:max_size 10M      # Максимальный размер 10MB
pm2 set pm2-logrotate:retain 7         # Хранить 7 файлов
pm2 set pm2-logrotate:compress true    # Сжимать старые логи
pm2 set pm2-logrotate:rotateInterval '0 0 * * *'  # Каждый день в полночь

# Проверка настроек
pm2 get pm2-logrotate
```

---

## 🔍 Диагностика проблем

### Бот не запускается

```bash
# 1. Проверьте логи ошибок
pm2 logs viaizer-bot --err --lines 50

# 2. Проверьте .env файл
cat .env

# 3. Проверьте зависимости
npm list

# 4. Попробуйте запустить напрямую
node bot.js

# 5. Проверьте порт
netstat -tlnp | grep :3000
```

### Бот падает

```bash
# 1. Проверьте логи
pm2 logs viaizer-bot --lines 100

# 2. Проверьте использование памяти
pm2 monit

# 3. Проверьте дисковое пространство
df -h

# 4. Перезапустите бота
pm2 restart viaizer-bot
```

### Нет ответа от бота

```bash
# 1. Проверьте статус
pm2 status viaizer-bot

# 2. Проверьте что бот запущен
ps aux | grep "node.*bot.js"

# 3. Проверьте логи
pm2 logs viaizer-bot --lines 50

# 4. Проверьте соединение с Telegram
curl https://api.telegram.org/bot$(grep BOT_TOKEN .env | cut -d'=' -f2)/getMe
```

---

## 📈 Мониторинг

### Создание скрипта мониторинга

```bash
cat > /root/viaizer/monitor.sh << 'EOF'
#!/bin/bash

echo "=== Viaizer Bot Status ==="
echo "Time: $(date)"
echo ""

# Статус PM2
echo "PM2 Status:"
pm2 status viaizer-bot
echo ""

# Использование памяти
echo "Memory Usage:"
pm2 jlist | grep -o '"memory":[0-9]*' | grep -o '[0-9]*' | awk '{print $1 / 1024 / 1024 " MB"}'
echo ""

# Uptime
echo "Uptime:"
pm2 jlist | grep -o '"pm_uptime":[0-9]*' | grep -o '[0-9]*' | awk '{print $1 / 60 " minutes"}'
echo ""

# Диск
echo "Disk Usage:"
df -h /root | tail -1 | awk '{print $5 " used"}'
echo ""

# CPU
echo "CPU Usage:"
top -bn1 | grep "Cpu(s)" | awk '{print $2}' | sed 's/%us,//'
echo ""
EOF

chmod +x /root/viaizer/monitor.sh

# Запуск мониторинга
/root/viaizer/monitor.sh
```

---

## 🚨 Алерты (опционально)

### Создание скрипта алертов

```bash
cat > /root/viaizer/alert.sh << 'EOF'
#!/bin/bash

BOT_TOKEN=$(grep BOT_TOKEN /root/viaizer/.env | cut -d'=' -f2)
CHAT_ID="your_chat_id"  # Замените на ваш chat_id

# Функция отправки сообщения в Telegram
send_alert() {
  local message="$1"
  curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
    -d "chat_id=${CHAT_ID}" \
    -d "text=${message}"
}

# Проверка статуса бота
BOT_STATUS=$(pm2 status viaizer-bot | grep viaizer-bot | awk '{print $10}')

if [ "$BOT_STATUS" != "online" ]; then
  send_alert "⚠️ Бот не работает! Статус: $BOT_STATUS"
fi

# Проверка использования памяти
MEMORY_USAGE=$(pm2 jlist | grep -o '"memory":[0-9]*' | grep -o '[0-9]*')

if [ $MEMORY_USAGE -gt 400000000 ]; then
  MEMORY_MB=$((MEMORY_USAGE / 1024 / 1024))
  send_alert "⚠️ Высокое потребление памяти: ${MEMORY_MB}MB"
fi
EOF

chmod +x /root/viaizer/alert.sh

# Добавление в cron для ежеминутной проверки
(crontab -l 2>/dev/null; echo "* * * * * /root/viaizer/alert.sh >> /root/viaizer/logs/alert.log 2>&1") | crontab -
```

---

## 📦 Бэкапы

### Создание скрипта бэкапа

```bash
cat > /root/viaizer/backup.sh << 'EOF'
#!/bin/bash

BACKUP_DIR="/root/backups/viaizer"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="viaizer_backup_${DATE}.tar.gz"

# Создание директории для бэкапов
mkdir -p $BACKUP_DIR

# Создание бэкапа
tar -czf "${BACKUP_DIR}/${BACKUP_FILE}" \
  /root/viaizer/.env \
  /root/viaizer/package.json \
  /root/viaizer/logs/ \
  --exclude='node_modules'

# Удаление бэкапов старше 30 дней
find $BACKUP_DIR -name "viaizer_backup_*.tar.gz" -mtime +30 -delete

echo "✅ Бэкап создан: ${BACKUP_DIR}/${BACKUP_FILE}"
EOF

chmod +x /root/viaizer/backup.sh

# Добавление в cron для ежедневного бэкапа в 2:00 ночи
(crontab -l 2>/dev/null; echo "0 2 * * * /root/viaizer/backup.sh >> /root/viaizer/logs/backup.log 2>&1") | crontab -
```

---

## 🎯 Чек-лист после миграции на PM2

- [x] PM2 установлен
- [x] ecosystem.config.js создан
- [x] Директория logs создана
- [x] .env файл проверен
- [x] Старый процесс остановлен
- [x] Бот запущен через PM2
- [x] PM2 конфигурация сохранена
- [x] Автозапуск настроен
- [x] Ротация логов настроена
- [x] Мониторинг настроен (опционально)
- [x] Бэкапы настроены (опционально)
- [x] Алерты настроены (опционально)

---

## 📞 Полезные ссылки

### PM2 документация
- https://pm2.keymetrics.io/docs/usage/quick-start/
- https://pm2.keymetrics.io/docs/usage/log-management/

### Telegram Bot API
- https://core.telegram.org/bots/api

### Node.js
- https://nodejs.org/docs/

---

## 🆘 Частые проблемы

### PM2 не сохраняет процессы после перезагрузки

```bash
# Повторно настройте автозапуск
pm2 save
pm2 startup systemd -u root --hp /root
```

### Логи не ротируются

```bash
# Переустановите pm2-logrotate
pm2 uninstall pm2-logrotate
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
pm2 set pm2-logrotate:compress true
```

### Бот не отвечает

```bash
# Перезапустите бота
pm2 restart viaizer-bot

# Проверьте логи
pm2 logs viaizer-bot --lines 50

# Проверьте .env файл
cat .env
```

---

## ✅ Успешная миграция!

После выполнения всех шагов ваш бот:

✅ Автоматически перезапускается при падении
✅ Запускается при старте системы
✅ Имеет управляемые логи с ротацией
✅ Может быть легко мониторирован
✅ Имеет возможность бэкапа
✅ Может отправлять алерты (если настроено)

---

## 📝 Следующие шаги

1. 📊 Настройте Redis для сессий
2. 🔒 Создайте отдельного пользователя для бота
3. 🔧 Добавьте health check endpoint
4. 🌐 Получите SSL сертификат
5. 🔄 Настройте CI/CD
6. 📈 Интегрируйте с системой мониторинга

---

*Документ создан: 2026-01-04*
*Автор: AI Assistant*
