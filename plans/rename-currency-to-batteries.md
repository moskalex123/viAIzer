# План: Переименование валюты "CRED" в "батарейки"

## Обзор задачи
Необходимо переименовать внутреннюю валюту бота с "CRED" на "батарейки":
- В коде и документации использовать название "батарейки"
- В сообщениях пользователю использовать эмодзи 🔋

## Файлы для изменения

### 1. bot.js (основной файл бота)

#### Изменение 1.1: Кнопка покупки подписки (строка 161)
**Было:**
```javascript
{ text: '🪙 Купить подписку / CRED', callback_data: 'buy_subscription' }
```

**Стало:**
```javascript
{ text: '🪙 Купить подписку / 🔋', callback_data: 'buy_subscription' }
```

#### Изменение 1.2: Опции подписок (строки 412-416)
**Было:**
```javascript
inline_keyboard: [
  [{ text: '💎 Премиум на месяц - 299 CRED', callback_data: 'sub_premium_30d' }],
  [{ text: '💎 Премиум на год - 2999 CRED', callback_data: 'sub_premium_365d' }],
  [{ text: '🪙 Купить CRED', callback_data: 'buy_credits' }]
]
```

**Стало:**
```javascript
inline_keyboard: [
  [{ text: '💎 Премиум на месяц - 299 🔋', callback_data: 'sub_premium_30d' }],
  [{ text: '💎 Премиум на год - 2999 🔋', callback_data: 'sub_premium_365d' }],
  [{ text: '🪙 Купить 🔋', callback_data: 'buy_batteries' }]
]
```

#### Изменение 1.3: Отображение баланса в профиле (строка 966)
**Было:**
```javascript
💰 Баланс: ${session.balance.toFixed(1)} CRED
```

**Стало:**
```javascript
💰 Баланс: ${session.balance.toFixed(1)} 🔋
```

#### Изменение 1.4: Описание подписок на русском (строка 993)
**Было:**
```javascript
subscription_options: '💎 <b>Подписки</b>\n\nВыберите подписку:\n\n📅 <b>На месяц</b> - 299 CRED\n• 1000 запросов/день\n• Все модели\n• Приоритетная поддержка\n\n📅 <b>На год</b> - 2999 CRED\n• Экономия 589 CRED\n• Все преимущества месячной подписки'
```

**Стало:**
```javascript
subscription_options: '💎 <b>Подписки</b>\n\nВыберите подписку:\n\n📅 <b>На месяц</b> - 299 🔋\n• 1000 запросов/день\n• Все модели\n• Приоритетная поддержка\n\n📅 <b>На год</b> - 2999 🔋\n• Экономия 589 🔋\n• Все преимущества месячной подписки'
```

#### Изменение 1.5: Описание подписок на английском (строка 1014)
**Было:**
```javascript
subscription_options: '💎 <b>Subscriptions</b>\n\nChoose a subscription:\n\n📅 <b>For a month</b> - 299 CRED\n• 1000 requests/day\n• All models\n• Priority support\n\n📅 <b>For a year</b> - 2999 CRED\n• Save 589 CRED\n• All benefits of monthly subscription'
```

**Стало:**
```javascript
subscription_options: '💎 <b>Subscriptions</b>\n\nChoose a subscription:\n\n📅 <b>For a month</b> - 299 🔋\n• 1000 requests/day\n• All models\n• Priority support\n\n📅 <b>For a year</b> - 2999 🔋\n• Save 589 🔋\n• All benefits of monthly subscription'
```

#### Изменение 1.6: Обработчик callback_data (строка 415)
**Важно:** Также нужно обновить обработчик callback_data в функции `handleCallbackQuery` (строки 216-244), чтобы обрабатывать новый callback_data `buy_batteries` вместо `buy_credits`.

**Было (примерно):**
```javascript
case 'buy_credits':
  // обработка покупки credits
  break;
```

**Стало:**
```javascript
case 'buy_batteries':
  // обработка покупки батареек
  break;
```

---

### 2. DEPLOYMENT.md

#### Изменение 2.1: Описание функционала (строка 54)
**Было:**
```markdown
- CRED balance management
```

**Стало:**
```markdown
- батарейки balance management
```

---

### 3. plans/integration-summary.md

#### Изменение 3.1: Описание системы подписок (строка 10)
**Было:**
```markdown
- Premium subscription system with CRED currency
```

**Стало:**
```markdown
- Premium subscription system with батарейки currency
```

#### Изменение 3.2: Таблица валют (строка 42)
**Было:**
```markdown
viAIzer: CRED currency
```

**Стало:**
```markdown
viAIzer: батарейки currency
```

#### Изменение 3.3: Описание общей системы валют (строка 106)
**Было:**
```markdown
- **unified_currencies**: Shared balance system (CRED + Batteries + Stars)
```

**Стало:**
```markdown
- **unified_currencies**: Shared balance system (батарейки + Batteries + Stars)
```

---

### 4. plans/text-ai-database-analysis.md

#### Изменение 4.1: Описание общего баланса (строка 377)
**Было:**
```markdown
2. **Shared Balance**: Use batteries/credits across both platforms
```

**Стало:**
```markdown
2. **Shared Balance**: Use batteries/батарейки across both platforms
```

#### Изменение 4.2: Описание унификации валют (строка 431)
**Было:**
```markdown
2. **Unify Currency**: Convert between CRED and batteries
```

**Стало:**
```markdown
2. **Unify Currency**: Convert between батарейки and batteries
```

---

### 5. README.md

#### Изменение 5.1: Описание функционала (строка 15)
**Было:**
```markdown
- Subscription system with CRED currency
```

**Стало:**
```markdown
- Subscription system with батарейки currency
```

---

### 6. plans/text-ai-synergy-plan.md

#### Изменение 6.1: Описание функционала (строка 34)
**Было:**
```markdown
- Subscription system with CRED currency
```

**Стало:**
```markdown
- Subscription system with батарейки currency
```

#### Изменение 6.2: Описание премиум-системы (строка 55)
**Было:**
```markdown
└── Premium System (CRED currency)
```

**Стало:**
```markdown
└── Premium System (батарейки currency)
```

#### Изменение 6.3: Описание общего баланса (строка 91)
**Было:**
```markdown
├── shared_credits_balance
```

**Стало:**
```markdown
├── shared_батарейки_balance
```

#### Изменение 6.4: Описание общей биллинговой системы (строка 98)
**Было:**
```markdown
- Unified billing and credits
```

**Стало:**
```markdown
- Unified billing and батарейки
```

#### Изменение 6.5: SQL запрос (строка 206)
**Было:**
```sql
COALESCE(t.credits, 0) + COALESCE(v.credits, 0) as total_credits
```

**Стало:**
```sql
COALESCE(t.батарейки, 0) + COALESCE(v.батарейки, 0) as total_батарейки
```

---

### 7. plans/unified-integration-architecture.md

#### Изменение 7.1: Описание ограничений (строка 26)
**Было:**
```markdown
❌ CRED currency isolated to this platform
```

**Стало:**
```markdown
❌ батарейки currency isolated to this platform
```

#### Изменение 7.2: Описание типов валют (строки 120-121)
**Было:**
```sql
cred_balance DECIMAL(10, 2) DEFAULT 0,      -- viAIzer currency
batteries_balance DECIMAL(10, 2) DEFAULT 0,   -- Text AI currency
```

**Стало:**
```sql
батарейки_balance DECIMAL(10, 2) DEFAULT 0,      -- viAIzer currency
batteries_balance DECIMAL(10, 2) DEFAULT 0,   -- Text AI currency
```

#### Изменение 7.3: Описание курсов обмена (строки 125-126)
**Было:**
```sql
cred_to_batteries_rate DECIMAL(10, 4) DEFAULT 1.0,
batteries_to_cred_rate DECIMAL(10, 4) DEFAULT 1.0,
```

**Стало:**
```sql
батарейки_to_batteries_rate DECIMAL(10, 4) DEFAULT 1.0,
batteries_to_батарейки_rate DECIMAL(10, 4) DEFAULT 1.0,
```

#### Изменение 7.4: Описание отслеживания заработка (строки 129-130)
**Было:**
```sql
total_cred_earned DECIMAL(10, 2) DEFAULT 0,
total_batteries_earned DECIMAL(10, 2) DEFAULT 0,
```

**Стало:**
```sql
total_батарейки_earned DECIMAL(10, 2) DEFAULT 0,
total_batteries_earned DECIMAL(10, 2) DEFAULT 0,
```

#### Изменение 7.5: Описание стоимости (строки 178-179)
**Было:**
```sql
cost_in_cred DECIMAL(10, 4),
cost_in_batteries DECIMAL(10, 4),
```

**Стало:**
```sql
cost_in_батарейки DECIMAL(10, 4),
cost_in_batteries DECIMAL(10, 4),
```

#### Изменение 7.6: Описание общей стоимости (строки 238-239)
**Было:**
```sql
total_cost_cred DECIMAL(10, 4),
total_cost_batteries DECIMAL(10, 4)
```

**Стало:**
```sql
total_cost_батарейки DECIMAL(10, 4),
total_cost_batteries DECIMAL(10, 4)
```

#### Изменение 7.7: SQL запрос вставки (строка 269)
**Было:**
```sql
INSERT INTO unified_currencies (user_id, cred_balance, batteries_balance)
VALUES ($1, 25, 100)
```

**Стало:**
```sql
INSERT INTO unified_currencies (user_id, батарейки_balance, batteries_balance)
VALUES ($1, 25, 100)
```

#### Изменение 7.8: SQL запрос выбора (строка 295)
**Было:**
```sql
SELECT cred_balance, batteries_balance, stars_balance
FROM unified_currencies
```

**Стало:**
```sql
SELECT батарейки_balance, batteries_balance, stars_balance
FROM unified_currencies
```

#### Изменение 7.9: Отображение баланса (строки 500-502)
**Было:**
```javascript
`💰 Unified Balance:\n\n` +
`🪙 CRED: ${balance.cred_balance}\n` +
`🔋 Batteries: ${balance.batteries_balance}\n`
```

**Стало:**
```javascript
`💰 Unified Balance:\n\n` +
`🔋 батарейки: ${balance.батарейки_balance}\n` +
`🔋 Batteries: ${balance.batteries_balance}\n`
```

---

## Порядок выполнения

1. **Сначала обновить bot.js** (основной файл бота)
   - Заменить все "CRED" на "🔋" в пользовательских сообщениях
   - Заменить callback_data "buy_credits" на "buy_batteries"
   - Добавить обработчик для нового callback_data

2. **Затем обновить документацию** (в любом порядке)
   - DEPLOYMENT.md
   - README.md
   - plans/integration-summary.md
   - plans/text-ai-database-analysis.md
   - plans/text-ai-synergy-plan.md
   - plans/unified-integration-architecture.md

3. **Проверить работу бота**
   - Запустить бота
   - Проверить отображение профиля
   - Проверить меню подписок
   - Убедиться, что все сообщения корректно отображают 🔋

---

## Важные примечания

- **Эмодзи 🔋** используется только в сообщениях пользователю
- **Название "батарейки"** используется в коде и документации
- **callback_data** должен быть на английском языке (buy_batteries)
- Проверьте все места, где упоминается валюта, чтобы ничего не пропустить
- После изменений протестируйте все функции, связанные с валютой

---

## Проверка после изменений

После выполнения всех изменений убедитесь, что:

1. ✅ В профиле пользователя отображается "Баланс: X.X 🔋"
2. ✅ В меню подписок цены указаны в 🔋
3. ✅ Кнопки подписок работают корректно
4. ✅ Вся документация обновлена
5. ✅ Нет оставшихся упоминаний "CRED" или "credits" в коде
6. ✅ Бот работает без ошибок
