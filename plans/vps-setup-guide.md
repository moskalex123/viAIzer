# 🔑 Настройка беспарольного SSH доступа к VPS

## 📋 Информация о VPS

- **IP-адрес:** 217.119.129.239
- **Пользователь:** root
- **Пароль:** 9sTUF001ug33
- **Новый пользователь:** botuser
- **Новый SSH порт:** 2222

---

## 📝 Шаг 1: Подключение к VPS

Откройте терминал (PowerShell, CMD, Git Bash или Termux на Android) и выполните:

```bash
ssh root@217.119.129.239
```

Когда попросят пароль, введите: `9sTUF001ug33`

✅ **Ожидаемый результат:** Успешное подключение к VPS как root

---

## 📝 Шаг 2: Обновление системы

После успешного подключения выполните:

```bash
apt update && apt upgrade -y
```

✅ **Ожидаемый результат:** Система обновлена

---

## 📝 Шаг 3: Установка базовых пакетов

```bash
apt install -y curl wget git vim nano htop tmux ufw fail2ban unzip build-essential python3 python3-pip software-properties-common
```

✅ **Ожидаемый результат:** Все пакеты установлены

---

## 📝 Шаг 4: Создание пользователя botuser

```bash
# Создание пользователя
adduser botuser
```

Система попросит:
- Введите пароль для botuser (придумайте надежный пароль)
- Повторите пароль
- Полное имя (можно нажать Enter)
- Номер комнаты (Enter)
- Рабочий телефон (Enter)
- Другой телефон (Enter)
- Корректно? (нажмите Y)

```bash
# Добавление пользователя в sudo группу
usermod -aG sudo botuser

# Создание директории для SSH ключей
mkdir -p /home/botuser/.ssh
chmod 700 /home/botuser/.ssh
```

✅ **Ожидаемый результат:** Пользователь botuser создан и добавлен в sudo группу

---

## 📝 Шаг 5: Настройка SSH ключей (на вашей локальной машине)

**Откройте НОВЫЙ терминал** на вашем компьютере/Android и выполните:

### На Windows (PowerShell):

```powershell
# Шаг 5.1: Создание директории для SSH ключей
if (!(Test-Path -Path "$env:USERPROFILE\.ssh")) {
    New-Item -ItemType Directory -Path "$env:USERPROFILE\.ssh" -Force
}

# Шаг 5.2: Генерация SSH ключа
ssh-keygen -t ed25519 -f "$env:USERPROFILE\.ssh\vps_bot_key" -C "botuser@vps"
```

Нажмите Enter для всех вопросов (использовать настройки по умолчанию)

```powershell
# Шаг 5.3: Проверка создания ключей
Get-ChildItem -Path "$env:USERPROFILE\.ssh"
```

Вы должны увидеть:
- `vps_bot_key` (приватный ключ)
- `vps_bot_key.pub` (публичный ключ)

```powershell
# Шаг 5.4: Копирование публичного ключа на VPS
type "$env:USERPROFILE\.ssh\vps_bot_key.pub" | ssh root@217.119.129.239 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Введите пароль root: `9sTUF001ug33`

### На Windows (Git Bash):

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -f ~/.ssh/vps_bot_key -C "botuser@vps"
```

Нажмите Enter для всех вопросов

```bash
# Копирование публичного ключа на VPS
ssh-copy-id -i ~/.ssh/vps_bot_key.pub root@217.119.129.239
```

Введите пароль root: `9sTUF001ug33`

### На Android (Termux):

```bash
# Установка openssh если нет
pkg install openssh

# Генерация SSH ключа
ssh-keygen -t ed25519 -f ~/.ssh/vps_bot_key -C "botuser@vps"

# Копирование ключа (вручную)
cat ~/.ssh/vps_bot_key.pub
```

Скопируйте вывод и вставьте в файл `/root/.ssh/authorized_keys` на VPS

✅ **Ожидаемый результат:** Публичный ключ скопирован на VPS

---

## 📝 Шаг 6: Перемещение ключей для пользователя botuser

**На VPS** (в первом терминале, где вы подключены как root):

```bash
# Проверяем, что ключи скопированы
cat /root/.ssh/authorized_keys

# Копируем ключи из root в botuser
cp /root/.ssh/authorized_keys /home/botuser/.ssh/authorized_keys

# Устанавливаем правильные права
chmod 600 /home/botuser/.ssh/authorized_keys
chown -R botuser:botuser /home/botuser/.ssh

# Проверяем права
ls -la /home/botuser/.ssh/
```

✅ **Ожидаемый результат:** Ключи скопированы и права установлены правильно

---

## 📝 Шаг 7: Тест беспарольного доступа

**На вашей локальной машине** (во втором терминале):

```bash
# Тест подключения с ключом
ssh -i ~/.ssh/vps_bot_key botuser@217.119.129.239
```

✅ **Ожидаемый результат:** Подключение без запроса пароля

Если подключение прошло успешно - введите `exit` чтобы отключиться

---

## 📝 Шаг 8: Настройка SSH конфигурации

**На VPS** (в первом терминале как root):

```bash
# Редактирование конфигурации SSH
nano /etc/ssh/sshd_config
```

Найдите и измените следующие параметры (или добавьте в конец файла):

```bash
# Измените порт
Port 2222

# Запрет root логина
PermitRootLogin no

# Запрет аутентификации по паролю
PasswordAuthentication no

# Разрешить только ключи
PubkeyAuthentication yes

# Максимум 3 попытки авторизации
MaxAuthTries 3

# Таймаут сессии
ClientAliveInterval 300
ClientAliveCountMax 2
```

Сохраните файл:
- Нажмите `Ctrl+O`
- Нажмите `Enter`
- Нажмите `Ctrl+X` для выхода

✅ **Ожидаемый результат:** Конфигурация сохранена

---

## 📝 Шаг 9: Перезапуск SSH

```bash
# Перезапуск SSH сервиса
systemctl restart sshd

# Проверка статуса
systemctl status sshd
```

✅ **Ожидаемый результат:** SSH сервис перезапущен успешно

---

## 📝 Шаг 10: Настройка Firewall (UFW)

```bash
# Разрешить новый SSH порт
ufw allow 2222/tcp

# Включить firewall
ufw enable

# Проверить статус
ufw status verbose
```

✅ **Ожидаемый результат:** Firewall включен, порт 2222 разрешен

---

## 📝 Шаг 11: Тест подключения через новый порт

**На вашей локальной машине**:

```bash
# Подключение через новый порт
ssh -i ~/.ssh/vps_bot_key -p 2222 botuser@217.119.129.239
```

✅ **Ожидаемый результат:** Успешное подключение через порт 2222

Если работает - введите `exit`

---

## 📝 Шаг 12: Настройка SSH config на вашей машине

**На вашей локальной машине**:

### На Windows/Linux/Mac:

```bash
# Создание или редактирование config файла
nano ~/.ssh/config
```

Добавьте следующие строки:

```
Host vps-bot
    HostName 217.119.129.239
    Port 2222
    User botuser
    IdentityFile ~/.ssh/vps_bot_key
    ServerAliveInterval 60
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

### На Android (Termux):

```bash
nano ~/.ssh/config
```

Добавьте те же строки и сохраните.

---

## 📝 Шаг 13: Тест подключения через алиас

**На вашей локальной машине**:

```bash
# Теперь можно подключаться просто
ssh vps-bot
```

✅ **Ожидаемый результат:** Мгновенное подключение к VPS

---

## 📝 Шаг 14: Настройка часового пояса (опционально)

**На VPS**:

```bash
# Установка часового пояса Москва
timedatectl set-timezone Europe/Moscow

# Проверка
timedatectl
```

---

## 📝 Шаг 15: Настройка Fail2Ban

**На VPS**:

```bash
# Копирование конфигурации
cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local

# Редактирование
nano /etc/fail2ban/jail.local
```

Найдите секцию `[sshd]` и измените:

```ini
[sshd]
enabled = true
port = 2222
maxretry = 3
bantime = 3600
findtime = 600
```

Сохраните и выйдите.

```bash
# Перезапуск Fail2Ban
systemctl enable fail2ban
systemctl start fail2ban

# Проверка статуса
fail2ban-client status
fail2ban-client status sshd
```

✅ **Ожидаемый результат:** Fail2Ban активен и защищает SSH

---

## ✅ Проверка результата

После завершения всех шагов:

### На VPS:
```bash
# Проверка статуса SSH
systemctl status sshd

# Проверка firewall
ufw status

# Проверка Fail2Ban
fail2ban-client status
```

### На вашей локальной машине:
```bash
# Тест подключения
ssh vps-bot

# Должно подключиться мгновенно без пароля
```

---

## 🎯 Быстрые команды после настройки

### Подключение к VPS:
```bash
ssh vps-bot
```

### Проверка статуса на VPS:
```bash
systemctl status sshd
ufw status
fail2ban-client status
```

### Просмотр логов SSH:
```bash
journalctl -u sshd -f
```

---

## ⚠️ Важные примечания

1. **После шага 9** вы больше не сможете подключиться как root с паролем
2. **Используйте только SSH ключи** через пользователя botuser на порту 2222
3. **Сохраните SSH ключи** в безопасном месте
4. **Не делитесь приватным ключом** (`vps_bot_key` без .pub)
5. **Сохраните пароль botuser** в надежном месте

---

## 🔄 Если что-то пошло не так

### Не работает SSH после изменения порта:

Подключитесь через VPS панель хостинга (консоль) и исправьте:

```bash
# Вернуть стандартный порт
nano /etc/ssh/sshd_config
# Измените Port 2222 на Port 22

# Перезапустить SSH
systemctl restart sshd
```

### Не работают SSH ключи:

Проверьте права на файлах:

```bash
# На VPS
ls -la /home/botuser/.ssh/
# Должно быть:
# -rw-------  botuser botuser  authorized_keys
# drwx------  botuser botuser  .
```

Если права неправильные:

```bash
chmod 700 /home/botuser/.ssh
chmod 600 /home/botuser/.ssh/authorized_keys
chown -R botuser:botuser /home/botuser/.ssh
```

---

## 📞 Следующие шаги

После успешной настройки беспарольного доступа:

1. ✅ Сообщите мне, что все шаги выполнены
2. 📦 Установка Node.js через NVM
3. 🗄️ Установка Redis и PostgreSQL
4. 🚀 Развертывание проекта
5. ⚙️ Настройка PM2

---

*Гид создан: 2026-01-01*
